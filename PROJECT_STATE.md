# CompanionLiteV2 Project State

最后更新：2026-08-14
当前版本：`2.1.2`
当前阶段：2.1.2 桥接正式字段收口，尚未发布
运行模式：`active`

## 当前结论

polite_silence 上游 1.2.0 已合入私聊独立概率字段。当前桥接在 active 模式下把 `trigger_percent_private` 运行时置为 `0`，群聊的 `trigger_percent` 保持不变；原先按完整提示片段从 system prompt 中摘除注入的过渡逻辑已经移除。Companion 状态机继续决定何时提示主模型输出 `<ignore>`，插件自身解析拒答标签、维护拒答事件，并在对方拒答结束回来时一次性告知主模型沉默时长。

2.0.2 在稳定版运行语义与编辑式管理台之上，收紧轻、深分析之间的关系语义一致性，并约束深分析印象只写主观感受、低置信度不落地。主人格提示使用“稳定 system 协议 + 动态五标签状态”：静态协议要求先决定是否愿意回答，动态状态告诉主人格本轮答多少、站多近、处于什么情况、内心怎么想以及如何表达。Companion 不注入人格侧情绪保护护栏。

`处境` 现在只负责待处理的具体互动事件；为空时写“当前无待处理的具体互动事件”，不会再暗示 `感受` 中不存在厌恶。正式绑定受限时进一步按原因区分“边界优先 / 问题优先 / 感受优先”。其他窗口即使存在真实好感，也只保持普通投入，不继承正式身份、偏心细讲或暧昧许可。

陌生阶段以当前编译器的“只用两三句直接回答当前问题”为硬上限：只给关键结论或方向，不写成长教程、完整推导、路线大全或额外资料。第 2 轮轻分析确认连续索取后，下一轮若继续投递新任务，投入、处境、感受与表达共同要求拒绝；连续六轮仍不承接互动后，新任务全部拒绝，只保留对具体承接或修复的回应。

负向关系保持原数值阈值，但表达改为克制尖锐、逐步收门，强烈厌恶时冷硬退出而不主动追击。正向关系不改变分数和增长速度，熟悉度与正亲和会更早表现为熟络、关心和有限偏心；暧昧、伴侣称谓和排他身份仍只属于正式绑定。

可靠修复复用现有问题阶段：第一次中高置信修复进入 `repairing` 并软化一级，第二个不重叠的两轮窗口再次确认后清除问题。轻、深分析共享同一修复进度，不新增数据库字段。

深分析提示升级为 `deep-v7`，`impression` 只写当前对对方的第一人称感受，禁止复述或转写谁做了什么、谁关心了谁等事件。首次确认负向模式且旧感受为空时必须提交非空第一人称感受；代码同时为模型漏字段、`keep` 或清洗后空文本提供基于已裁决状态的确定性兜底，它不重新理解原始对话。低置信度分析不再采纳模型的 `revise` / `clear` 印象操作，既有感受保留，缺失时由兜底接管。首次两轮单向观察只生成较轻的暂态不满，六轮确认后才进入明确关门；修复阶段会同步软化旧感受，所有低置信关系模式均不得落地。27,648 种动态状态继续在最低 260 字预算内保持五标签完整。

切换 `active` 前发现的两处状态硬伤已经关闭：

- 同一目标深轮次由每 UMO 的统一分析锁串行，应用阶段再次拒绝 `target_round <= last_deep_round`，不会重复扣分。
- 正式绑定窗口禁止档案重建，且重建流程不再重新套用 `35 / 60 / 15` 的首次绑定托底。

调试页提供“关系档案 / 启停管理”两个内部页签。关系档案采用编辑式人物侧写，依次展示关系结论、六轮互动节奏、五标签语义投影、消息时间线和默认折叠的工程诊断；单会话启停开关已从档案页移除。管理视图继续负责统计、搜索、筛选、排序和逐会话即时开关。五秒自动刷新会保留页签、折叠、消息展开、滚动、交互焦点和正在提交的行；360px 下不产生横向滚动。

LivingMemory 继续独立负责用户事实和长期记忆；CompanionLite V2 只调整投入、距离与边界。

## 位置

源码：

```text
plugins/astrbot_plugin_companion_lite_v2
```

运行实例：

```text
data/plugins/astrbot_plugin_companion_lite_v2
```

配置：

```text
data/config/astrbot_plugin_companion_lite_v2_config.json
```

数据库：

```text
data/plugin_data/astrbot_plugin_companion_lite_v2/companion_lite_v2.db
```

调试页：

```text
http://127.0.0.1:6185/#/plugin-page/astrbot_plugin_companion_lite_v2/debug
```

## 当前运行状态

| 项目 | 当前值 |
|---|---|
| `operation_mode` | `active` |
| `max_message_length` | `400` |
| `max_buffer_rounds` | `24` |
| `max_context_chars` | `340` |
| 反思模型 | 当前配置的模型提供商 |
| 运行版本 | `2.1.2` |
| 主人格注入 | 已启用 |
| 礼貌性沉默桥接 | 已启用并接管（`trigger_percent = 0`） |
| 正式绑定记录 | `0` |
| 关系档案 | `2` |
| 已保存消息 | `2` |

重置故障已通过真实 HTTP 路径 API 验证：状态、消息、待处理交互和绑定会一起清空，不在公开文档记录实际 UMO 或会话数据。

## 2.1.x 桥接与维护

### 礼貌性沉默桥接

桥接链由一个开关控制（`bridge_polite_silence`，默认关闭）。开启且处于 `active` 时，插件探测 polite_silence 1.2.0+ 的 `trigger_percent_private` 并在运行时置 0，避免两套私聊触发并存；群聊使用的 `trigger_percent` 不变。原私聊概率会被记录，开关关闭或插件卸载时还原；若用户在接管期间手动改过私聊概率，退出时保留用户的新值。未安装 polite_silence、缺少正式私聊字段、`observe` 模式或开关关闭时整条链 no-op。

拒答注入时机由 Companion 状态机决定，而不是概率：

- `disengaged` 必注入（建议 90 分钟）；
- `guarded` 且活动问题为 `boundary_violation / degradation / coercion`、阶段为 `noticed / expressed` 时注入（建议 30 分钟）；
- `light_guidance.reminder == hold_boundary` 时注入；
- `repairing` 阶段一律不注入，给修复留出空间。

提示携带真实 sender_id 与建议时长，只说明“可以选择礼貌性沉默”及指令格式，最终是否输出 `<ignore>` 由主模型自主决定；polite_silence 的时长上下限与白名单仍然生效。拒答指令与恢复告知追加在 system_prompt 尾部：前缀（人格与固定协议）保持字节稳定，无注入轮次的 prompt 缓存不受影响；sender_id 统一做引号清洗，避免破坏 `<ignore>` 标签属性。

响应侧以 `priority=10` 先于 polite_silence 取得未清理文本，解析 `<ignore>` 标签（自闭合与双标签、属性大小写不敏感，取 id 与 duration），确认 polite_silence 已注册才记录，避免幽灵事件；入库前清洗 `<ignore>` 标签，避免分析缓冲混入 XML。事件写入 `last_silence_event`（target_id、duration_minutes、source_round、created_at）并累加 `silence_count`，随 `companion_state` 落库，不需要数据库迁移。能再次收到该用户消息即视为拒答已结束（含管理员手动解封），此时一次性告知主模型“上一轮沉默了 X 分钟”，随后清除事件，不重复注入。`silence_count` 本轮只用于记录、展示与诊断，不参与关系数值计算。

### WebUI 与状态展示

- 顶部状态条改为低调的“实时 / 已暂停”指示，新增桥接状态徽章（已接管 / 待接管 / 未启用 / observe 不接管 / 未安装），只展示不跳转。
- 启停管理新增“联动插件”区块，拨动开关调用 `POST /page/silence_bridge`，写入 AstrBot 配置并立即同步运行时；与配置面板读同一份配置。
- 移除“感受注入追踪”卡片，异步时序提示移入工程诊断；关系侧写新增拒答信号，工程诊断区展示 `last_silence_event` 原始字段。
- 六轮大总结周期卡补充周期起点、已推进轮数与周期消息数，运行元信息下沉至周期卡。
- `/clv2_status` 增加拒答统计行（累计次数 + 最近一次时长与轮次）。

### 维护

main.py 由 1509 行拆分为 500 行入口与 `core/` 服务模块：`persona.py`、`silence_bridge.py`、`reflection_service.py`、`webui.py`、`commands.py`、`protocol.py`、`web.py`。拆分顺手修复了 `core/webui.py` 与 `llm` 之间的循环导入，协议常量下沉到 `core/protocol.py` 切断反向依赖。`reflection_service.perform_locked` 拆分为准备、标记运行、应用结果三个阶段；调试页 Web API 增加统一异常兜底，handler 异常收敛为 JSON 错误响应，不再裸 500。

## 主人格提示词语义审计

### 审计目标

本轮同时审计主人格实际收到的关系块和 `severe / light / deep` 的交接边界。判断标准是：

- botname 能否扫一眼就知道本轮答多少、站多近、是否可以表达不悦；
- 每句话是否只承担一个职责；
- 是否存在重复安全声明、抽象术语或互相冲突的权限；
- 是否在 LivingMemory 等其他动态内容存在时仍保持明确边界。

### 稳定系统协议

```text
<companion_protocol version="4">
解释动态五字段的职责与执行顺序，并要求拒绝时不要先交付答案。
</companion_protocol>
```

协议逐轮保持字节稳定并前置到 system prompt。每轮动态块不再重复协议说明，因此稳定前缀可用于模型侧 Prompt Cache。

### 五个字段

| 字段 | 唯一职责 |
|---|---|
| `投入` | 本轮回答多少 |
| `关系` | 与当前用户站多近 |
| `处境` | 待处理的具体互动事件；为空时写“当前无待处理的具体互动事件”，不否定感受 |
| `感受` | 第一人称内心态度；无感受时写“无已形成的明确感受” |
| `表达` | 语气、情绪表达权限和轻提醒 |

五个标签始终完整输出。关系总结、问题摘要、三维数字、人格 ID、UMO 和内部标签只供调试。

### 关键语义

- 陌生：只用两三句直接回答当前问题，只给关键结论或方向，不写成长教程、完整推导或额外资料。
- 首次轻分析确认单向：若下一轮继续投递新任务，投入明确拒绝且不提供答案；处境、感受和表达使用同一关系结论，不再保留中性许可。
- 六轮持续单向：新任务全拒，不提供答案或要点，只回应具体承接或修复。
- 负向：从不耐烦、克制尖锐到强烈厌恶、冷硬退出，不主动升级冲突。
- 正向：初识后逐步显出熟络、关心和偏心，但未绑定时不预设亲密或排他身份。
- 单向 `reserved`：若本轮仍是知识请求或任务，直接拒绝，不提供答案、要点、教程或后续展开邀请。
- 负亲和：在所有熟悉阶段直接降低帮助意愿并增强厌烦、反感或敌意；低信任单独限制额外投入。
- 正常绑定：当前对象是唯一正式承认的亲密对象，对其有明确但不过度预支的偏爱；可以自然主动或回应亲近、暧昧和昵称。
- 绑定但受限：`guarded / disengaged` 时边界优先，存在活动问题时问题与关系距离优先，仅因负亲和、低信任或 `reserved` 时感受与关系距离优先。
- 其他窗口高亲和：`感受` 可保留真实好感，但当前窗口保持普通投入；答全当前问题即可，不额外细讲、包办、使用正式身份或许可暧昧。
- 修复：直接写“可以稍微缓和，但暂不恢复额外投入或亲近”，不再使用“降低对抗感”。
- 严重问题：直接描述贬低、威胁、强迫或边界施压；任务拒绝权限由姿态映射提供。

“主动劳动”“保护选择权”“降低对抗感”等需要二次解释的词已经移除。轻提醒统一去除尾部标点，不会再出现 `。。`。

### 主观感受

第一人称感受由 `deep-v7` 深分析维护并进入主人格请求。小模型被要求写“我不想再理他了”式内心态度，不得用“我观察到互动偏单向”式分析报告代替；印象只写对对方的感受，不复述谁做了什么。首次确认负向模式而旧感受为空时，`keep` 不再是合法语义；低置信度分析不采纳模型的印象改写。若模型仍漏写，代码根据已经裁决的关系模式生成第一人称兜底，并在诊断决策中标记 `impression_source=fallback`。

### 实测长度

| 黄金状态 | 字符数 |
|---|---:|
| 全部 27,648 状态最短 | `156` |
| 全部 27,648 状态平均 | `198.0` |
| 全部 27,648 状态最长 | `260` |

配置预算继续限制为 `260..340`，避免现有配置迁移。最低 260 字能完整保留全部五标签及其语义。

## 主链路加固

- 后台反思、`/clv2_reflect`、Web 手动反思和档案重建共享每 UMO 的统一分析锁。
- 同轮并发测试中两个第六轮深分析请求只调用一次模型、只结算一次，最终保持 `2 / 48 / -4`。
- 正式绑定重建在任何模型调用前返回 `bonded_rebuild_forbidden`，档案和消息完全不变。
- 未绑定和曾绑定档案仍可在 `observe` 下原子重建。
- 重置与分析锁串行，清空完整 UMO 的状态、消息、待处理交互、反思队列和正式绑定；Debug 页用刷新代次阻止旧响应回填。
- 每个完整 UMO 对应一份独立关系状态与消息缓冲；`conversation_id` 只用于人格解析。
- Active 注入把字节稳定的 `companion_protocol` 前置到现有 system prompt，并把 `companion_state` 作为一个 `mark_as_temp()` 内容块排在已有 LivingMemory 等动态内容之后。
- 同一事件重复 Hook 时，静态协议和动态状态都只出现一次；不支持临时内容块时仅动态状态回退到当前请求末尾。

## 验证结果

- 源码 141 项测试通过；Ruff 通过；源码与运行实例同步一致。
- 桥接专项测试覆盖：`<ignore>` 标签解析（自闭合 / 双标签 / 属性大小写 / 缺字段 / 无标签）、事件写入、恢复告知只注入一次后清除、接管置 0 与关闭还原、各姿态与问题组合的触发判断（含 `repairing` 不注入）。
- WebUI 手工验收：启停管理拨开关后徽章变为“已接管”且配置面板同键同步；未安装 polite_silence 时徽章显示未安装；感受注入追踪卡片消失；关系侧写出现拒答摘要；`/clv2_status` 输出拒答行。
- AstrBot 插件开发指南合规审计：元数据、插件命名、数据目录、动态提示词临时内容块、Page Bridge、依赖声明、Logo、Ruff 与包体限制均通过。
- 源码：104 项测试通过；Ruff 通过。
- 稳定版基线：100 项测试通过；Ruff 通过。
- Ruff：源码与运行实例均通过。
- 本次修改的源码、文档、页面和测试文件哈希：源码与运行实例同步一致。
- 27,648 状态语义编译矩阵：五标签完整，最低 260 字预算通过；`156..260` 字，平均 `198.0`，独特输出 `331`；拒绝权限、关系距离、处境、感受和表达之间的矛盾检查为 `0`。
- 20 轮全链路模拟：通过。
  - 轻分析：第 `2 / 4 / 8 / 10 / 14 / 16 / 20` 轮；
  - 深分析：第 `6 / 12 / 18` 轮；
  - 每轮只注入一份关系块；
  - 100% 普通消息不调用严重确认模型；
  - 第六轮后状态为 `2 / 48 / -4 / reserved / one_sided`。
- 正式绑定重建零调用与状态不变：通过。
- 调试页请求途中开始选择文本的竞态保护：自动化结构检查通过。
- Debug WebUI 通过 Bridge 健康数据正常加载；公开记录仅使用 `persona-name` 等泛化标识，不保留实际人格 ID、UMO 或档案内容。
- 关系档案不再含 UMO 启停卡片或 `/enabled` 调用；管理页仍是唯一启停入口。
- 编辑式关系侧写、六轮进度、五标签投影、原文复制、消息时间线和折叠诊断已在实际页面验证。
- 语义投影页签、原文与诊断展开状态及键盘焦点经过五秒自动刷新后保持不变。
- 双页签 ARIA、视图切换、会话搜索、状态筛选、统计同步和详情跳转通过实际页面验证。
- 管理页临时将一个已关闭 UMO 开启，统计由 `1 / 1` 同步为 `2 / 0`，随后恢复为原始 `1 / 1`；没有批量入口或批量接口。
- 360px 实测管理面板 `scrollWidth == clientWidth`，会话卡片与逐行开关均完整可见。
- 页面控制台没有 CompanionLite 报错；仅观察到 AstrBot 市场页原有的分类翻译警告。
- LivingMemory、Favour Ultra、Sylanne 及其他插件的源码和数据：未修改。

## 已知限制与后续

1. 继续在真实聊天中观察陌生私聊是否保持两三句直接回答，以及六轮持续单向后是否拒绝全部新任务。
2. 动态五字段仍属于当前 user message 的附加内容，但其解释和执行规则已由稳定 system 协议赋权。
3. 持久感受由深分析或严重事件维护；轻分析可以在有效窗口内编译较轻的暂态感受，但不会把短期观察直接写成长期印象。
4. `severe / light / deep` 的交接语义已审计；分析模型仍可能提交不理想文本，因此关系权限和空感受不变量继续由代码兜底。
5. `pending_interaction.fail_interaction()` 仍未接入无最终回复或取消 Hook，长期可能积累不参与分析的待定消息。
6. 本地严重候选筛查只负责低成本召回；隐晦、跨语言和跨多轮侵犯仍主要依赖后续分析。
7. `silence_count` 本轮只用于记录、展示与诊断，不参与关系数值计算；后续如需让拒答频率影响状态，需要另行设计结算规则。
8. 桥接以“再次收到该用户消息”判定拒答已结束，无法区分自然结束与管理员手动解封。
9. 拒答指令与恢复告知追加在 system_prompt 尾部，触发轮会牺牲该轮一次 prompt 缓存命中；无注入轮次的稳定前缀缓存不受影响。

## 重新接手时

```text
1. 先看本文件和 Debug WebUI
2. 确认运行模式仍为 active
3. 用一条真实私聊检查“最近注入”是否变为“是”
4. 观察主人格是否按新五字段语义行动
5. 用真实对话复测第 3 轮条件拒绝和第 6 轮持久感受
6. 桥接开启时确认徽章为“已接管”、触发轮提示含拒答指令、拒答事件入库、对方回来只注入一次恢复告知
7. 不修改其他插件或 LivingMemory 数据
```
