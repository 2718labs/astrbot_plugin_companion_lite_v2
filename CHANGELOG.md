# Changelog

本项目遵循语义化版本风格；alpha 阶段允许调整内部数据结构和提示协议，但 V2 始终不迁移或修改 V1 与 LivingMemory 的数据。

## 2.0.1 - 2026-07-28

### 关系语义一致性

- 陌生档回答上限由“可以用两三句”改为“只用两三句”，避免把篇幅限制解释成可选建议。
- 陌生档删除容易诱导扩写的“总览”，改为只用两三句直接回答当前问题。
- 第 2 轮轻分析确认连续索取后，`投入 / 处境 / 感受 / 表达` 使用同一条件分支；模式继续时直接拒绝且不提供答案，不再把拒绝软提示追加在中性表达之后。
- 活动问题优先于短期提醒；关系尚浅、边界维持和修复阶段不再与偏爱、暧昧许可或空处境同时出现。
- 深分析升级为 `deep-v6`；首次确认负向模式且旧感受为空时必须提交非空第一人称感受，代码对漏字段、`keep` 和清洗后空文本提供确定性兜底。
- 严重事件、负向姿态、负亲和、低信任及正亲和在缺少模型感受时均能编译出相符态度；被代码驳回的关系模式不能修改感受。
- 首次两轮单向观察使用较轻的暂态不满，六轮确认后才进入“不想继续单方面投入”；不再用同一句兜底混淆观察与定案。
- 第一次具体修复会把旧的关门感受改为观察修复，第二次独立确认后按剩余姿态和亲和保留适量不满，不再让已清除问题与旧感受互相冲突。
- 深分析的低置信关系模式一律不得建立问题或负向感受；明确辱骂、贬低、威胁和强迫仍按原高置信预检处理，不扩张含混阴阳或玩笑的词面识别。
- 语义矩阵扩展到 27,648 种组合，并加入跨标签权限、距离、处境、感受和表达一致性断言。

## 2.0.0 - 2026-07-28

### 首个稳定版本

- 完成从 `2.0.0-alpha.1` 到 `2.0.0-alpha.12` 的关系状态、轻重分析、修复流程、正式绑定与五标签语义编译验证。
- 固化编辑式关系档案与独立启停管理双视图，保留逐会话启停、自动刷新保护和移动端响应式布局。
- 7,680 种动态状态在最低 260 字预算下保持五标签完整；源码 100 项测试与 Ruff 检查通过。
- V2 使用独立插件 ID、数据库、配置、命令、Web 路由与 GitHub 仓库，可与 V1 保留源码，但不得同时注入关系上下文。
- 移除已弃用的 AstrBot 插件注册装饰器；按 AstrBot 插件开发指南补齐标准 `desc` 元数据、命令说明、规范 PNG 品牌图、忽略规则、许可证与开发检查配置。
- 公开文档和测试夹具统一使用 `botname`、`persona-name` 与合成 ID，不提交实际 UMO、人格 ID、本机路径、模型提供商或运行数据。
- 发布包不包含本地配置、真实会话数据库、缓存、日志或其他运行数据。

## 2.0.0-alpha.12 - 2026-07-28

### 编辑式关系档案

- 关系档案后半截重组为关系侧写、互动节奏、语义投影、互动记录和工程诊断，降低同权白卡与原始字段堆叠。
- 关系侧写突出姿态、实际阶段、正式身份、当前问题、关系总结和第一人称感受；空的辅助字段不再铺满占位符。
- 六轮周期改为可视进度，下一轮与上一轮提示词合并为五标签语义投影，原始文本支持展开与复制。
- 最近消息改为纵向时间线，超过六条时默认收起较早记录；工程诊断默认折叠，命中预检或分析错误时首次自动展开。
- 五秒同步新增保存语义页签、消息展开、诊断与原文折叠、滚动和交互焦点。

### 启停分责

- 删除关系档案中的“陪伴功能管理”卡片、单会话开关及对应请求分支；独立“启停管理”成为唯一启停入口。
- 管理页逐行开关、`POST page/enabled`、加载锁和失败回滚保持不变；未新增 API、数据库或构建依赖。
- 包内版本与插件 metadata 统一提升为 `2.0.0-alpha.12`。

## 2.0.0-alpha.11 - 2026-07-28

### 处境分责与跨窗口投入

- 空 `<处境>` 改为“当前无待处理的具体互动事件”，明确没有活动事件不等于没有厌恶；第一人称态度继续只由 `<感受>` 承担。
- 正式绑定按边界、活动问题或当前感受分别编译关系优先级，不再在没有活动问题时凭空写“当前问题”。
- 其他绑定窗口可保留真实好感，但投入降为普通窗口水平，不再因高亲和获得偏心细讲或额外包办。
- `companion_protocol v4`、五标签、数值阈值、修复状态机、分析协议和数据库结构均未变化。

### 双视图管理台

- Debug Dashboard 新增“关系档案 / 启停管理”内部页签；详情视图保持原结构，管理视图集中展示最多 1000 个已知 UMO。
- 管理视图增加总量、启用、关闭、启用比例统计，以及搜索、状态筛选、排序、移动端卡片布局和逐项即时开关。
- 管理页继续复用单 UMO `/enabled` 接口；请求中只锁定当前行，失败保持原状态，不提供批量启停。
- 包内 `__version__` 与插件 metadata 统一为 alpha.11，避免健康接口与插件列表版本不一致。

## 2.0.0-alpha.10 - 2026-07-28

### 关系语义与修复节奏

- 明确以当前编译器的“两三句简单总览”作为陌生档正式行为；保留 alpha.9 历史记录，不回写旧版本说明。
- 连续六轮只索取任务且不承接互动后，新任务全部拒绝，不提供答案或要点，只回应具体承接或修复；继续忽视已表达偏好时升级为 `guarded`。
- 负亲和保持原阈值，表达改为克制尖锐、逐步收门；强烈厌恶时冷硬退出，不再授权主动敌对或持续攻击。
- 正向分数、阈值和增量不变；熟悉度与正亲和会逐步表现为熟络、自然关心和有限偏心，未绑定时仍禁止预支伴侣称谓和排他身份。
- 第一次中高置信修复进入 `repairing` 并软化姿态，第二个不重叠的两轮窗口再次确认后清除问题；轻、深分析共享同一修复阶段，无数据库迁移。

### Debug 控制面板

- Debug 页采用现代化卡片层级并增加感受注入追踪，区分当前档案、上一轮实际快照和下一轮预览。
- “陪伴功能管理”删除重复状态字符串，改为带状态点、运行摘要、完整 UMO 和开关的紧凑控制条；修复控制区与上一行信息紧贴的问题。
- 全套 94 项测试和 Ruff 检查通过；7,680 状态矩阵在 260 字预算下为 `150..260` 字。

## 2.0.0-alpha.9 - 2026-07-27

### 陌生语感与重置按钮修复

- 陌生阶段投入从"一律拒绝、不给答案"调整为"最多一句话"：要你讲题、总结、分析、推导、比较或整理资料的直接拒；一句话能答完的事实或现实危险提醒可以回，但不展开、不教学、不附赠第二步。语气更接近 botname 本人，不再像规则手册。
- 修复 Debug 页"重置本会话"按钮完全无响应的根因：AstrBot 插件页 iframe 的 sandbox 不含 `allow-modals`，`window.confirm()` 被浏览器静默拦截并返回 `false`，导致 `apiPost` 从未发出。改为按钮内联二次点击确认，不再依赖任何原生弹窗。
- 修复运行实例 `context_builder.py` 与源码漂移的问题：上一轮只同步了版本号和重置路由，但陌生硬拒绝文案和协议 v4 未进入运行副本，实机仍在用 alpha.7 的许可式措辞。本轮已逐文件比对哈希并同步。
- 全套 82 项测试通过。

## 2.0.0-alpha.8 - 2026-07-27

### 重置链路实修

- Debug 重置不再依赖 POST body 解析，改用 `page/reset/<path:user_id>` 路由直接携带完整 UMO；保留旧 body 路由供兼容调用使用。
- 重置响应增加删除消息数、删除轮数、剩余消息数、姿态和当前轮次；前端只有收到 `ok: true` 才视为成功，否则显示错误。
- 用运行数据库中的真实 UMO 完成 HTTP 端到端验证：删除 32 条消息和 16 轮交互，重置后为 `0 / 50 / 0 / normal`，消息、待处理交互和绑定均为 0。

## 2.0.0-alpha.7 - 2026-07-27

### 人格感受与可靠重置

- `companion_protocol` 升至版本 3，动态状态增加 `<感受>`；深分析生成的第一人称内心态度经过清洗和 40 字限制后进入主人格。
- 深分析提示升级为 `deep-v5`，明确要求输出“我不想再理他了”式主观态度，禁止用关系分析报告代替感受。
- `reserved + one_sided` 遇到后续知识请求时改为直接拒答，不再允许先给一个要点，也不得附加教程或继续展开邀请。
- Debug 重置与反思分析串行，清空真实反思队列、关系状态、消息缓冲、待处理交互和该 UMO 的正式绑定。
- Debug 刷新增代次校验和强制刷新，重置前发出的旧请求不能覆盖重置后的界面，刷新中的重置也不会丢失后续刷新。
- 明确缓存隔离键为完整 UMO；`conversation_id` 只用于人格解析。
- 全套 81 项测试通过。

## 2.0.0-alpha.6 - 2026-07-27

### 独立关系裁决

- `companion_protocol` 升至版本 2，动态状态从五字段收敛为 `<投入> / <关系> / <处境> / <表达>` 四字段，完整删除 `<避免>` 与人格侧情绪保护规则。
- 投入改为硬上限：先决定是否愿意回答；拒绝时不先交付大部分答案，也不要求附赠教程、替代方案或安抚。
- 陌生人不再默认获得知识型服务；认识阶段最多点拨一两个方向。负亲和与低信任在所有熟悉阶段直接限制投入。
- 姿态优先级固定高于亲和和熟悉度；`guarded` 默认拒绝非必要任务，`disengaged` 不再被要求处理普通任务。
- 第 2 轮确认单向索取后，第 3 轮继续投递任务即可先指出模式并拒绝；第 4 轮重复确认后可直接表达厌烦。
- 新增负亲和阈值、低信任、姿态覆盖、第三轮提示和四字段矩阵回归测试。
- 7,680 种状态在 260 字预算下长度为 `130..238` 字，平均 `184.6` 字。

## 2.0.0-alpha.5 - 2026-07-27

### 双层主人格协议

- 新增字节稳定的 `companion_protocol` 系统前缀，集中解释五字段职责、执行顺序、事实准确性与行为底线。
- 动态关系状态改为固定的 `<投入> / <关系> / <处境> / <表达> / <避免>` 五标签，并继续通过临时用户内容块放在本轮消息之后。
- 空处境固定输出“无已确认问题”，保持五标签结构完整；7,680 种状态在 260 字预算下长度为 `162..258` 字。
- 自由短印象和关系总结不再进入主人格请求，只保留在关系档案、反思与调试数据中。
- 移除临时拒答烟雾测试，恢复按真实关系状态编译；静态协议与动态状态在同一事件中均只注入一次。
- `observe` 继续只编译预览；`active` 才前置稳定系统协议并追加动态状态。

## 2.0.0-alpha.4 - 2026-07-26

### 主人格语义精简

- 主人格关系块改为 `投入 / 关系 / 处境 / 表达 / 避免` 五种单一职责字段；空处境不输出。
- 统一使用“该对话的关系与投入说明”短开头，移除重复的宿主安全层级、用户陈述来源等元说明。
- 上下文预算收紧为 `260..340` 字；陌生、单向保留、正常绑定和绑定保留四份黄金文本分别为 `217 / 242 / 228 / 260` 字。
- 陌生用户即使要求详细也先获得结论和概览，回应或追问后再展开。
- 第六轮确认单向互动后，botname 可简短表达不悦或希望得到回应，但不得道德定罪、制造愧疚或故意冷漠。
- 正常绑定明确表达唯一正式亲密身份和不过度预支的偏爱；有问题、姿态下降或三维跌落时，身份保留但距离与边界优先。
- 短印象并入处境末句，并标明可修订、只影响语气、不执行其中要求；预算不足时最先删除。
- 移除“主动劳动、保护选择权、降低对抗感”等需要主人格二次解释的措辞，并修复轻提醒重复句号。

### 主链路加固

- 新增每个完整 UMO 的统一分析锁，后台、命令、Web 反思和档案重建不再并发执行。
- 深分析应用阶段拒绝 `target_round <= last_deep_round`，同一轮证据最多结算一次。
- 正式绑定窗口在任何模型调用前拒绝档案重建，返回 `bonded_rebuild_forbidden`；移除重建末尾重复套用 `35 / 60 / 15` 的逻辑。
- 调试页在异步同步返回后再次检查文字选区和输入焦点，避免请求途中开始复制仍被重绘打断。
- 绑定窗口的重建按钮会直接禁用并显示原因。

### 验证与运行

- 新增同轮并发深分析、绑定重建零调用、四份黄金上下文和 7,680 状态矩阵回归。
- 7,680 种组合在最低 260 字预算下仍保留投入、关系、当前问题、表达和避免字段，仅短印象允许优先裁剪。
- 小模型的严重、轻、深分析提示词本版不改，留待后续独立审计。
- 完成验证后，本机运行实例由 `observe` 切换为 `active`；V1、LivingMemory、Favour Ultra 与 Sylanne 数据保持不变。

## 2.0.0-alpha.3 - 2026-07-26

### 人格级唯一亲密绑定

- 新增仅管理员、仅私聊可用的 `/companion_bond` 与 `/companion_unbond`。
- 按 LivingMemory 的实际优先级独立解析人格：会话覆盖、当前 conversation、默认人格；不形成运行时依赖，也不把 UMO 前缀误作人格。
- 新增 `companion_bond` 排他表，以人格 ID 确保同一人格同时只有一个正式亲密窗口。
- 首次绑定只把不足的三维托到“刚到偏爱”：熟悉度 `35`、信任 `60`、亲和 `15`；不会降低更高数值，也不会清除姿态、问题或印象。
- 幂等绑定不重复加分；解除后重新绑定也不再次托底。
- 解除绑定只收回正式身份，完整保留三维、问题、姿态、印象和消息历史，并标记为曾绑定。
- `/clv2_reset` 仍只重置当前档案，不删除人格级绑定。

### 精简关系语义

- 主人格上下文加入简短的正式关系说明，不暴露人格 ID、UMO、布尔值或内部标签。
- 绑定窗口可以自然亲近、暧昧和使用昵称，但绑定不代表服从，也不强迫温柔。
- `reserved / guarded / disengaged` 与未解决问题始终覆盖亲密表达。
- 其他窗口维持普通关系距离，不透露绑定者身份；曾绑定窗口按熟人距离回应，不默认沿用伴侣称谓。
- 修正高熟悉、低信任或低亲和时的冲突表述，统一描述为“互动时间较长，但默契和亲近仍有限”。
- 340 字编译器改为先保证所有高优先级短块存在，再按剩余预算升级措辞；没有问题时不再浪费字符声明“无问题”。

### 陌生阶段的过早亲密

- 新增 `premature_intimacy` 观察信号，用于陌生或认识阶段的明确表白、暧昧或预设亲密身份。
- 普通友善、称赞、玩笑和自然示好不算过早亲密；正式绑定窗口的正常亲密表达豁免。
- 普通过早亲密只在下一次深分析扣亲和 `-2`，不直接升级姿态，同一六轮窗口最多结算一次。
- 表达距离后继续施压仍进入既有边界路径；性骚扰、强迫和威胁不因绑定而豁免。

### 调试与验证

- 调试页新增完整 UMO、实际人格 ID、解析来源、当前窗口身份、管理员可见占用 UMO、绑定时间和下一轮绑定语义预览。
- 调试页轮询放慢到五秒；数据未变化时不重绘，选中文字或操作输入框时暂停同步，复制内容不再被刷新打断。
- 轻、深分析提示分别升级到 `light-v4` 与 `deep-v4`，动态区仅增加早期关系和正式亲密两个布尔事实。
- 源码通过 69 项测试及 Ruff 静态检查。
- 默认仍保持 `observe`，不会自动向主人格注入上下文。
- V1、LivingMemory、Favour Ultra 与 Sylanne 的源码和数据均未修改。

## 2.0.0-alpha.2 - 2026-07-26

### 分析职责重构

- 小模型职责收窄为关系证据观察，不再直接输出姿态、绝对三维数值或自由回复指导。
- 轻分析输出固定为 `signal + confidence + evidence`。
- 深分析输出行为模式、agent 是否实际表达、用户如何回应、离散三维变化标签、调试摘要和短印象。
- 姿态、问题阶段、三维变化、普通周期升级上限、修复门槛和严重事件例外统一改由代码裁决。
- 首次六轮确认单向索取固定结算为熟悉度 `+2`、信任 `-2`、亲和 `-4`、`one_sided / noticed / reserved`。
- 第 6 轮只有记录中确实出现 agent 表达且用户继续忽视或施压，才允许进入 `ignored_expression / guarded`；未表达时最多进入 `one_sided / reserved`。
- 泛泛道歉不能开启修复；缺少具体承认时，正向信任和亲和变化被代码阻止。

### 严重事件按需确认

- 新增每轮零成本本地严重候选筛查，正常消息不增加 LLM 调用。
- 候选覆盖直接人格贬低、威胁、强迫服务以及边界表达后的继续施压。
- 引用块和代码块优先排除；误命中只触发小模型复核，不直接修改关系。
- 严重确认限制当前消息最多 400 字、5 秒超时并失败开放。
- 每用户每六轮最多确认两次，同一规范化消息去重；已有严重问题后相同行为免复核。

### 三维关系与投入尺度

- 三维变化改为有限枚举：熟悉度 `0/+2/+5`，信任和亲和 `-10/-4/-2/0/+2`。
- 熟悉度改为只升不降；阶段阈值调整为陌生 `<10`、认识一些 `10..34`、熟悉 `35..64`、长期熟悉 `>=65`。
- 默认信任 50 的语义改为“尚未建立可依赖的信任”，不再预支基本信任。
- 未修复问题存在时禁止正向信任和亲和变化；修复阶段单周期最多各恢复 `+2`。
- 陌生人默认先获得结论和少量关键点；承接或继续追问后再展开。

### 提示词、缓存与上下文

- 严重确认、轻分析和深分析建立带版本号的稳定系统提示。
- 显式人格参考并入深分析静态前缀；动态区仅保留最小状态和截断对话。
- 轻分析不再接收旧印象、绝对三维数值或冗余关系总结。
- 深分析中的旧摘要和旧印象明确标记为待复核假说。
- 主人格上下文预算从 600 字缩减到 340 字，并按安全、投入、问题、表达、避免、短印象的顺序裁剪。
- 模型生成的关系总结和问题摘要改为仅供调试，不再注入主人格。
- 短印象限制为 60 字并增加指令化文本清洗。
- 超长模型证据改为在标点或词边界收口，避免截出半个英文单词。
- 记录提示词版本、动态输入字符、cached/uncached/output tokens、模型标签和代码裁决，不保存完整分析提示词。

### 调试、回放与运行稳定性

- 调试页新增三维数字与语义、本地严重筛查、确认调用与跳过原因、模型观察到代码裁决、缓存用量和下一轮编译预览。
- 下一轮编译预览与上一轮实际快照分开显示。
- 页面继续每两秒自动同步，并保持页面与长文本面板的滚动位置。
- 新增仅在 `observe` 可用的“保留消息并重建档案”；所有分析成功且期间无状态或消息变化时才原子替换。
- 修复 AstrBot 4.26 插件 Web API 请求体读取：从旧式 `get_json()` 改为 `request.json()`，恢复重建、重置和手动反思操作的 `user_id` 解析。
- 版本提升到 `2.0.0-alpha.2`，默认继续保持 `observe`。

### 验证

- 源码与运行实例均通过 53 项测试及 Ruff 静态检查。
- 100 条普通消息的严重确认 LLM 调用数为 0。
- 实机保留并重建测试 UMO 的 12 条消息，最终得到熟悉度 `2`、信任 `48`、亲和 `-4`、`one_sided / noticed / reserved`。
- 实机确认上下文未注入、运行模式仍为 `observe`。
- 本次实机深分析缓存命中率为 97.4%。
- V1 源码和 LivingMemory 数据保持不变。

## 2.0.0-alpha.1 - 2026-07-26

### 独立 V2 基线

- 在 V1 旁边建立完全独立的 `astrbot_plugin_companion_lite_v2`，不读取、不迁移、不修改 V1 数据。
- 建立独立插件 ID、注册名、显示名、命令、配置、数据库、数据目录和 Web 路由。
- 使用 `familiarity`、`trust`、`affinity` 三维关系，配合 `normal / reserved / guarded / disengaged` 四级姿态。
- 建立每两轮轻分析、每六轮深分析的固定调度。
- 建立固定标签到自然语言关系上下文的确定性编译。
- 提供 `observe` 与 `active` 两种运行模式，默认 `observe`。
- 私聊关系键改为 AstrBot 完整 UMO，不同私聊会话独立建档。
- 新增用户档案调试页、会话选择、消息缓冲和实时两秒同步。
- 单条用户消息和主人格回复默认最多入库 400 字，超长内容保留首尾并标记截断。
- 数据库仅保留 `companion_state`、`message_buffer` 和 `pending_interaction` 三张业务表。
